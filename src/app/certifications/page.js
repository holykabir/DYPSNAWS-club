import CertificationsPageClient from "./CertificationsPageClient";

export const metadata = {
  title: "Certifications | AWS Cloud Club",
  description: "Explore AWS certifications we help students achieve — from foundational to specialty.",
};

export default function CertificationsPage() {
  return <CertificationsPageClient />;
}
